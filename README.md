一个简易版的`MySQL`终端客户端。

### 一、命令选项
* `-H` `--host`: 主机IP
* `-P` `--port`: 端口，可选参数，默认值为 3306
* `-u` `--user`: 用户名
* `-p` `--passowrd`: 密码

*默认不支持SSL模式*

### 二、支持的SQL
**当前版本事务会自动提交**

`use, select, show, explain, create, update, delete, insert`

### 三、终端推出指令
* `exit`
* `quit`
* `q`
* `Q`